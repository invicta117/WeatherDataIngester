services:

  db:
    container_name: postgres
    image: postgres:latest
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_USER: weather
      POSTGRES_PASSWORD: ingester
    ports:
      - "5432:5432"